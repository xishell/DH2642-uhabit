# Local development environment variables for Wrangler/Cloudflare Workers
# Copy this file to .dev.vars and update with your own values
# This file should NOT be committed to git
#
# These variables are available at RUNTIME in your Cloudflare Worker

# Generate a secure secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-here

# Local dev server URL (default port for Cloudflare Pages dev)
BETTER_AUTH_URL=http://localhost:8788

# Development mode (optional - auto-enabled for localhost)
# Set to "true" to enable relaxed security settings:
# - Minimum password length: 4 characters (instead of 8+)
# - Extended session duration: 30 days (instead of 7)
# - No email verification required
# WARNING: Never enable this in production!
DEV_MODE=true
